<Window x:Class="WPF_ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gemma" Height="520" Width="900" WindowStartupLocation="CenterScreen" Background="#FF0B1220">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FF0E1722" Padding="12" CornerRadius="6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Gemma" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" />
                <TextBlock Text="  •  Desktop" Foreground="#FF9AA6B2" FontSize="13" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Main area -->
        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1.4*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Camera + controls -->
            <Border Grid.Column="0" Background="#FFFFFFFF" CornerRadius="8" Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Camera" FontWeight="SemiBold" FontSize="16" Foreground="#FF0E1722" />

                    <!-- Camera display area (placeholder) -->
                    <Border x:Name="CameraBorder" Background="#FF10161F" Height="280" CornerRadius="6" Margin="0,8,0,0">
                        <Grid>
                            <TextBlock x:Name="CameraPlaceholderText" Text="Camera feed not started" Foreground="#FF9AA6B2" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <Image x:Name="CameraImage" Visibility="Collapsed" Stretch="Uniform" />
                        </Grid>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
                        <Button x:Name="StartCamButton" Content="Start Camera" Width="110" Height="34" Margin="6" Click="StartCamButton_Click" />
                        <Button x:Name="StopCamButton" Content="Stop Camera" Width="110" Height="34" Margin="6" Click="StopCamButton_Click" IsEnabled="False" />
                    </StackPanel>

                    <!-- Microphone visual -->
                    <TextBlock Text="Microphone" FontWeight="SemiBold" FontSize="16" Foreground="#FF0E1722" Margin="0,14,0,0" />
                    <ProgressBar x:Name="MicLevelBar" Height="16" Minimum="0" Maximum="100" Value="0" Margin="0,6,0,0" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button x:Name="StartMicButton" Content="Start Mic" Width="110" Height="34" Margin="6" Click="StartMicButton_Click" />
                        <Button x:Name="StopMicButton" Content="Stop Mic" Width="110" Height="34" Margin="6" Click="StopMicButton_Click" IsEnabled="False" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right: Conversation (STT/TTS) -->
            <Border Grid.Column="1" Background="#FFFFFFFF" CornerRadius="8" Padding="12">
                <StackPanel>
                    <TextBlock Text="Conversation" FontWeight="SemiBold" FontSize="16" Foreground="#FF0E1722" />

                    <TextBlock Text="Transcript:" Margin="0,8,0,0" />
                    <TextBox x:Name="TranscriptBox" Height="120" TextWrapping="Wrap" IsReadOnly="True" Background="#FFF8F9FB" />

                    <TextBlock Text="Say something (STT)" Margin="0,8,0,0" />
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="StartSttButton" Content="Start STT" Width="100" Height="34" Margin="6" Click="StartSttButton_Click" />
                        <Button x:Name="StopSttButton" Content="Stop STT" Width="100" Height="34" Margin="6" Click="StopSttButton_Click" IsEnabled="False" />
                    </StackPanel>

                    <TextBlock Text="Text to speak (TTS)" Margin="0,10,0,0" />
                    <TextBox x:Name="TtsInputBox" Height="60" TextWrapping="Wrap" Background="#FFF8F9FB" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button x:Name="SpeakButton" Content="Speak" Width="110" Height="34" Margin="6" Click="SpeakButton_Click" />
                        <Button x:Name="StopSpeakButton" Content="Stop" Width="110" Height="34" Margin="6" Click="StopSpeakButton_Click" IsEnabled="False" />
                    </StackPanel>

                    <TextBlock Text="Notes: STT/TTS and camera require extra packages or platform APIs. Current build provides placeholders and mic visualization. See code-behind comments for setup instructions." Foreground="#FF6B7785" FontSize="11" Margin="0,12,0,0" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="© Gemma" Foreground="#FF9AA6B2" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Window>
